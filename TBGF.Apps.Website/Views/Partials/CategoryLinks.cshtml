@model IEnumerable<string>
@{
    var space = "";
    var result = "";
    foreach (var category in Model)
    {
        result += space + category;
        space = ",&#32;";
    }
}
@Html.Raw(result)